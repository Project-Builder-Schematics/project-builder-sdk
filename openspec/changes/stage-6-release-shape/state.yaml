# Orchestrator-owned DAG state — stage-6-release-shape
change: stage-6-release-shape
triage: L
sensitivity_override: true
completed_phases:
  - triage
  - explore
  - propose
current_phase: design
current_phase_status: complete — rev 2 (spec V2 signed upstream; design.md rev 2 + ADRs 0040-0042 draft + ADR-0034 amendment; architecture_impact: ADDITIVE [0 deviations, tarball-shrink documented deliberately]; 29 file rows, 3 flows, ~60 scenario derivations; council diff applied: DR-1 stub version 0.0.1 [council 2-0 over executor 0.0.0 — semver inversion], DR-2 OWNER RULING ritual + link:sdk convenience script [architect prepare-dissent recorded], DR-3 tech-writer filenames authoring-verbs/authoring-errors + docs/README.md index, DR-4 codegen bin no-flags contract pinned, DR-5 Bun native YAML.parse + bun-version pin note + broadened secret patterns + deprecation-check followup + tarball-shrink sentence)
spec_signed: V2 (owner 2026-07-12; 27 REQs/63 scenarios)
steward_foresight: ALIGNED (north-star.md + obs 908); 3 CQs owner-answered 2026-07-12 — CQ1 bun-link consumer LIVE today (significance affirmed), CQ2 placeholder DEFERRED to public-package plan (descope applied — spec PPH V3 signed, 6 active REQs/11 scen + PPH-07/08 deferred; design rev 3; ADR-0040 status Deferred; ZERO registry writes in this change), CQ3 external author out of near-term scope (reckoning bar adjusted)
slices: rev 1 — S-000..S-005 (skeleton bun-link e2e + 5 SPIDR; 25/25 active REQs, 52/52 scenarios; obs #909)
plan_verify: READY (iteration 3/3, 2026-07-12; verify-plan-1..3; trajectory 18→3→0 — iter 3 Judge A "no findings" + Judge B "ready to execute"). Slices FINAL = rev 3.
plan_complete: true
in_loop_iteration: 0
next_phase: /build (owner command; mandatory first scope S-000 walking skeleton — bun-link e2e vertical). Reckoning obligations carried: completed walkthrough-record demanded before archive (G-2); design §4.12 followup candidates (deprecation-check CI) registered at archive.
execution_mode: interactive
artifact_store: hybrid
spec_status: none
notes: >
  Triage L by council convergence (executor + blind architect + blind pm): two sensitivity
  overrides live (deployment/publish.yml + security/supply-chain tarball boundary) AND
  intrinsic L (4-5 bounded workstreams, ~15-20 files). Not XL: one cohesive artefact (the
  release shape), no program decomposition.
  OWNER AMENDMENT at sign-off (2026-07-12): release-READINESS, not release — NO live npm
  publish in this change (package not L1-ready until real engine integration reveals gaps);
  local consumption via bun link / packed tarball enters in_scope as THE install story;
  live publish explicitly out (owner gate, future). Distribution: zero-cost MANDATORY; owner
  direction = DUAL publish config (local bun-link/tarball + GitHub Packages prepared-not-fired);
  GH Packages scope-match constraint (@pbuilder vs repo owner) + install auth-wart routed to
  propose/design as ADR.
  Carried flags for explore/propose: (1) dist/core strip is NOT free — dist/testing imports
  ../core/context.js at runtime; strip requires resolving defineFactory graduation first
  (architect, ADR-worthy). (2) defineFactory graduation = weakest why-now, highest risk —
  justify at propose or split (pm). (3) FIT-09 is shape-only today; packed-install resolution
  must EXTEND test/e2e/installed-consumer.e2e.test.ts, not build a parallel vehicle. (4)
  Exports map + FIT-09 designed data-driven/additive so stage-5b's future dialects add rows,
  not rewrites. (5) Engine-gated ledger rows 74/56/142 must be re-tagged carried-forward,
  NEVER closed by this change. (6) bun link resolves against the live repo dir (no files/
  tarball filtering) — it needs dist built and does NOT prove tarball shape; the pack->
  install->resolve CI smoke stays as the tarball proof (publishes nothing). (7) PM tripwire:
  6+ ADRs at design => halt and re-triage. (8) UX-designer not activated (DX flow is
  tech-writer + BA territory); DBA not activated.
updated_at: 2026-07-12
