# Propose Council Record — `runner-tripwire-invariants` (L)

> Two blind lenses (opus, BA + PM), 2026-07-29, launched in parallel. Condensed faithful
> record. Both lenses INDEPENDENTLY refuted W1's emergency premise (see ruling 5 in
> explore-council.md — the correction is owner-ratified and binding).

## PM lens — value cliff & budget

**Framing**: the payoff is not "fewer bugs", it is "NO THIRD JUDGING ROUND". Partial credit
does not exist — a mechanism closing 90% of the tail buys 0% of the outcome. explore.md's
Approach 1 (per-guard positional predicates) does NOT buy the outcome: default stays pass;
QA's two live escapes survive it; PM confirmed a third structurally (`derive-runner-closure.ts:292`
classifies ANY `node:`-prefixed specifier as ordinary builtin → `node:child_process` passes).

**MINIMUM DURABLE SHAPE (fund this)**: M1 default-deny totality for the Constraint-4 guard;
M2 ONE totality fitness function (classified-count == present-count, red-proven) — M1+M2 are
not separable, that pair IS the change; M3 resolution-based containment/disjointness with
undecidable→violation; M4 fail-closed atomicity as ONE property (P4); M5 honest ViolationRule
+ whole-message assertions (⚠ contradicts signed REQ-CST-06.1 "asserted by substring" —
unfreeze it alongside 04.2/04.3); M6 the four owner-ruled primitives; M7 W2 test gate.

**GOLD-PLATING (deferred, with triggers registered)**: fitness fns capped at 4
(FIT-CAP-TOTALITY, path-spelling-invariance, fail-closed-biconditional, manifest-byte-
neutrality); deferred: CAP-ORACLE, NO-CHECKER-IN-BUILD, SINGLE-PREDICATE, RULE-REACHABILITY,
BASELINE-NOT-SELF-HEALING. Mutants: ~15-20 committed (confirmed-live escapes + one per
invariant + INVERSE/green), NOT 60. ts-morph in-memory mutation harness REJECTED (new test
infrastructure disguised as debt closure) — directory-enumerated corpus gets 80% at zero
machinery. Generated capability-baseline ARTEFACT declined (churn on ordinary edits;
rubber-stamping restores default-pass through the human) — take the PROPERTY, decline the
artefact [owner-ratified as ruling 8]. ADRs target 3: mechanism (supersedes/amends 0076),
totality class rule, resolution-based paths.

**Sequencing**: W1′ (explicit rebuild step) + W2 + R1-13 + react-timeout as **S-000, one
slice, independently mergeable** — fast-track value without a second pipeline's ceremony.
NON-NEGOTIABLE slice constraint: R1-17 (relaxing bare-`Function`) lands WITH callee-
decidability, NEVER before (relaxing first opens `const F = Function; F("…")`).

**Fix at proposal (re-opening at design = scope re-open)**: mechanism class (default-deny
totality — decided with the three live escapes as evidence); syntax-only decidability;
ambiguity⇒violation as a REQ, not prose; the P1-P5 property list in scope; ruling-1 shape
(primitives as scenario examples); W1 resolution + S-000 composition; no-high-churn-artefact
constraint; R1-15 in / R1-14 re-registered [ruled]; the budget gates; R1-17 sequencing.
**Defer to design**: ADR numbering/consolidation; R1-16 probe-then-close; predicate placement
(state it is NOT an ADR-0075 reversal); diagnostic register wording; corpus layout; semantic
oracle (default no, register as followup).

**Debt tracking**: 5 dispositions, each with mandatory evidence field — CLOSED-BY-MECHANISM
(named red-proof/mutant-class), CLOSED-BY-FIX (REQ-ID), ALREADY-DISCHARGED (commit+date),
RE-REGISTERED (rewritten fresh dated row — never "still open"), OUT-WITH-REASON. One row in →
exactly one row out; mechanism rows may not absorb independent rows by adjacency. THREE new
registrations owed: the two missing carry_to_archive items (0.1.0-must-ship-manifest
release-checklist row; user-reachable integrity-mismatch diagnostic) + every deferred fitness
fn/mutant with its trigger. Mechanical /evaluate check: register row-count delta fully
explained by the disposition table.

**Success criteria at /evaluate** (abridged): the two live escapes each ≥1 violation by rule
identity + exact count; the four primitives fail naming the primitive; totality structural +
red-proven; five disjointness spellings violate (=$VAR as explicit unclassifiable);
fail-closed biconditional over ≥3 faults against a PRE-SEEDED scratch root; manifest
byte-neutrality (violation ⇒ halt, change became cross-repo); 18 existing red-proofs green,
zero deletions without named survivor; publish.yml runs suite + explicit ordering + fit-23
green; register fully dispositioned; budget respected; **criterion 11: judgment-day returns
zero findings whose fix is "add another spelling"** — the only criterion that says whether we
bought anything.

## BA lens — users, capabilities, dispositions

**User value**: U1 maintainer (red gate diagnosable in a minute; green MEANS something);
U2 the AI agent/contributor whose commit trips a guard (true rule + true cause → resolution
is "write it differently", never "widen the exemption" — both prior CRITICALs lived in
exemptions); U3 engine team (closure-sealing lemma they can rely on); U4 future judge
("is the property closed?" answerable from a committed artefact); U5 end user (no bogus
integrity failure, no uncovered bytes); U6 register reader (every row leaves with a
disposition).

**Capabilities (spec spine)**: C1 capability-admission (CAP) — admitted uses declared,
everything else violation, undecidable = explicit unclassifiable-construct, fail-closed;
C2 classifier-totality (TOT) — inseparable from C1; C3 primitive-coverage (PRM) — ONE
register, ONE enforcement site, incl. the four ruled primitives + node:vm fold, every member
has a producing fixture; C4 exemption-as-proof-obligation (XPO) — file-level proofs,
forfeit-on-any-other-arrangement, covers BOTH the createRequire anchor AND the sanctioned
dynamic-import site; C5 spelling-invariant-path-verdicts (PTH); C6 fail-closed-generation-
totality (FCG) = P4 as one REQ; C7 diagnostic-honesty (DGN) — rule true of the violation,
whole-message assertions; C8 publish-path-integrity (PPI) — behavioural digests-vs-packed-
bytes proof + suite gate [reshaped by ruling 5]; C9 evidence-of-exhaustiveness (EVD) —
negation partition per invariant, collapsible into cross-cutting test mechanics at design;
C10 enforcement-documentation-fidelity (DOC) — counts derived not frozen (R1-11); C11
register-disposition-completeness (DBT). **Scope flag: 11 families is XL-shaped — C8 is a
separable subsystem → slice #1 independently shippable** [matches PM's S-000].

**Disposition divergences vs explore.md (all owner-resolved via rulings 3/7)**: R1-6 fixed
here (P4); R1-13 fixed here (C8); R1-8, R1-15, RMD-05.1/RMD-01.2 fixed here (ruling 7);
R1-14 re-registered with security's Tier-1 ranking recorded. NEW recommended-in (cheap under
C1/C4): M2.2 function-constructor chain (the cheapest proof C1 closed the CLASS), M1.12
re-export laundering (if real and left open, the change's central claim is false on day one),
M1.13 anchor drift (exemption pointing at an unwalked file is a dormant hole).

**Failure modes beyond green tests** (abridged): baseline-diff rubber-stamping; false
positives → exemption widening; "green still means nothing" (prose exhaustiveness); the tail
moves (label without the property); W2 satisfied in the letter (gate present, main still
unprotected — direct push publishes); docs land ahead of enforcement (C10 asserting more
than C1 enforces — the exact condition judgment-day named against 0.1.0); register sheds
rows silently; re-signed spec carries known-false statements [closed by ruling 7].

**Scenario seeds**: S1 unlisted-spelling escape fails naming the PROPERTY not a primitive
(distinguishes admission model from longer deny list); S2 totality as exact classified==
present equality on the real tree; S3 exemption forfeit on decoy+re-export arrangement +
green sibling (namespace form per the file's own header); S4 behavioural publish-sequence
digest proof (packed bytes, stamped version); S5 fail-closed biconditional against
PRE-SEEDED root across 6 faults; S6 primitives; S7 disjointness spellings; S8 totality
red-mutation; S9 publish gate rejects a violating closure BEFORE any publish step.
