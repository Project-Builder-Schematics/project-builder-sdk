# Slices: Stage 5b — Dialect Breadth

**Triage**: L · **Spec**: V2 signed (4 domains, 13 REQ-IDs, 47 scenarios) · **Design**: rev 2
**Total slices**: 6 (1 walking skeleton + 5 SPIDR) · **Revision**: 4 (plan-verify iter 3 fixes:
S-002↔S-003 seam — guard-loop/doc op-name split by ship order (`removeImport`+`addFunction` stay
in S-002, `addVariable`/`addClass` moved to S-003, the slice that ships them), S-003 cut clause
rewritten to pin delete-vs-edit mechanics (`ops-exact-set.test.ts` SURVIVES every cut, EDITED not
deleted); plan-verify iter 2 fixes: row-141 batch-grouping task assigned to S-002, S-003 cut
tripwire quantified, FIT-14 deep-equal bullet states both verified halves, S-005 leaf-rule
reworded to DOCUMENTED-LIMIT with no new scanner, baseline-regeneration procedure stated verbatim
in binding constraint 5; plan-verify iter 1 fixes: rows 139/145 self-contained, row-136 commit
granularity pinned, S-003 default/authority stated)
**Test**: `bun test <path>` · full `bun test` · types `bunx tsc --noEmit` · build `bun run build`
(needed for FIT-04/FIT-14 dist-diffing).

## Executor Context (mandatory, exempt from word budget)

### Mandatory reading (before any slice)

1. `openspec/changes/stage-5b-dialect-breadth/design.md` — §4.1 (seam), §4.2/4.2b/4.2c
   (file/flow/architecture changes — the contract this file slices), §4.3 (data model, op
   signatures), §4.4 (frozen error strings — copy verbatim), §4.5 (ADR-0039 + amendments to
   0037/0010/0012), §4.6 (Test Derivation — the REQ→test→file map), §4.8 (rollback invariants).
2. Signed delta specs: `specs/typescript-dialect/spec.md`, `specs/dialect-generics/spec.md`,
   `specs/modify-coalescing/spec.md`, `specs/dialect-conformance/spec.md` — REQ-IDs below
   resolve against these; this file is the build ORDER, those are the build CONTENT.
3. `triage.md` Owner Rulings #2/#3/#4 (collision-namespace pin, error-class pin, op signatures).
4. `src/core/dialect-handle.ts` (current: `runOp`/`runRaw`/`runModify`/`#ensureOpen` — the
   surface every S-000 task rewrites), `src/core/session.ts`, `src/core/context.ts`,
   `src/core/define-dialect.ts`, `src/dialects/typescript/{ops,ast,index}.ts` (current thin
   `addImport`-only shape).
5. `openspec/sensitive-areas.md` line 13 (`security (code execution)` row, `confidence: medium`)
   + line 21 (stale "All entries are `confidence: low` and anticipated" sentence) — S-002 fixes
   both.

### Binding constraints (verbatim from orchestrator brief — violating any is a slicing defect)

1. Walking skeleton (S-000) = `dialect-error.ts` + `deep-equal.ts` + shared `#invokeContained`
   wired through `runOp`+`runRaw` (byte-identical `raw() on "{path}" threw` preserved) + one
   trivial e2e proof. No new ops here.
2. Row-136: GREEN characterisation of today's silent LWW lands FIRST, then is REPLACED by the
   reject scenarios in the SAME slice (S-002) — never dropped silently.
3. `addVariable`/`addClass` (REQ-TSD-10/11) are isolated in their OWN slice (S-003) — cutting
   orphans nothing: their REQ-IDs, goldens, allow-list entries, doc-loop names all exit together.
4. `addFunction`±export (REQ-TSD-09, the named pain) lands as early as possible after the
   skeleton — hence S-001, immediately after S-000.
5. Each op slice (S-001/S-002/S-003) commits its own FIT-04 `.d.ts` + FIT-14 pkg-surface
   baseline rows in the SAME slice (rollback invariant, design §4.8). **Regeneration procedure
   (VERIFIED — no script exists for either baseline; both are hand-maintained)**: (i) run `bun
   run build` (produces fresh `dist/**`; both baselines diff against this — `package.json` has
   no dedicated regen script, only `test`/`typecheck`/`build`). (ii) **FIT-04**
   (`test/fitness/dts-baseline/*.d.ts`): hand-copy the slice's changed `dist/**/*.d.ts` file(s)
   over their committed counterpart, naming convention `dist/<segments>/<file>.d.ts` →
   `<segments-joined-by-dot>.<file>.d.ts` (e.g. `dist/dialects/typescript/index.d.ts` →
   `typescript.index.d.ts`; full pair list — commons, umbrella `index.d.ts`, conformance,
   core/handle-state, core/base-handle, core/authoring-error, commons/classify-content, testing,
   typescript — lives in `DTS_PAIRS` in `test/fitness/fit-04-dts-semver-gate.test.ts`). The check
   (`normalizeDeclarations` + `findBreakingRemovals`) is a comment/blank-line-stripped
   LINE-REMOVAL diff, NOT byte-identical — a purely additive new declaration line (a new op)
   never fails on its own; a baseline update is REQUIRED only when an EXISTING line's text
   changes, and is otherwise slice-owner hygiene to keep the committed baseline current (rollback
   invariant). (iii) **FIT-14** (`test/fitness/pkg-surface-baseline.json`): hand-edit the JSON —
   `exports`/`dependencies`/`files`/`bin` copied verbatim from the built `package.json` (`toEqual`
   — exact structural match required, any drift fails); `shebang` copied from
   `dist/bin/pbuilder-codegen.js`'s first line; `tarball` regenerated by running `bun pm pack
   --dry-run` and copying its sorted `packed <size> <path>` listing into the array (a
   new/missing-entry SET diff against `baseline.tarball`, not byte-order-sensitive, but every
   path must be accounted for — no stray or missing entries).
6. DAS-01.1 guard-flip (extend the positive shipped-verbs loop, DELETE the negative
   unshipped-surface test) + doc naming SPLIT by ship order: `removeImport`+`addFunction` land in
   S-002 (the `removeImport` slice — `addFunction` is already shipped by preceding S-001, so
   naming it here is safe); `addVariable`/`addClass` land in S-003, the SAME slice that ships
   them — never naming an unshipped verb in the interim (REQ-DAS-01: the loop names exactly the
   shipped API).
7. The dialectError-passthrough composition test (REQ-DG-06.5, collision message surviving
   `runOp` containment byte-exact) is OWNED by S-000 (the `runOp`-containment slice) — proven
   there with a synthetic `dialectError`-throwing test-fixture op, since no op-level collision
   reject exists yet at that point in the build.
8. Fail-closed (REQ-DG-07): the `RunContext.runFailure` poison flag + its F2 dependency-direction
   fitness guard land WITH the containment work (S-000), proven generically there (REQ-DG-07.3,
   synthetic op). REQ-DG-07.1 (row-136 trigger) proves out in S-002; REQ-DG-07.2 (collision
   trigger) proves out in S-001 — same mechanism, later exercised by the concrete trigger.
9. Registry deliverable (sensitive-areas medium→high promotion + stale-paragraph fix + guard
   assertions) is owned by S-002 — same test file (`security-authoring-guard.test.ts`) as the
   DAS-01.1 flip, per design's own File Changes row bundling both.
10. Mutation-gated `#ensureOpen` lands in S-000 (satisfies "before removeImport" — REQ-TSD-08.4/
    08.6 in S-002 exercise the mechanism with `removeImport`-specific scenarios; S-000 proves the
    generic zero-directive-for-a-true-no-op case using the pre-existing `addImport`).

### Multi-slice files (intentional, not orphans — see Coverage Check)

`src/dialects/typescript/{ops,index}.ts`, `test/core/dialect-handle.test.ts`,
`docs/authoring-a-dialect.md`, `test/fitness/fit-04-*`/`fit-14-*`,
`test/docs/security-authoring-guard.test.ts` (S-002 adds `removeImport`+`addFunction` to the
loop, S-003 adds `addVariable`+`addClass`), and `test/fixtures/red/**` are each touched
incrementally across multiple slices — every touch is listed under its owning slice below;
nothing is silently duplicated or dropped.

### Load-bearing literals (copy exactly)

- Foreign wrap: `dialect operation failed: {op}() on "{path}" threw` (`{op}` = `"raw"` for
  `runRaw` — byte-identical to today).
- Row-136 reject: `dialect operation failed: cannot .modify() "{path}" while a structured edit
  is pending on the same handle — the pending edit would be lost; call .read() to commit it
  first, then .modify()`.
- Add-op collision (one template, three call sites): `dialect operation failed: {op}("{name}")
  on "{path}" — a value or import binding named "{name}" already exists; TypeScript forbids two
  value declarations sharing a name. Rename or remove the existing one, or edit it with .raw().`
- Compose-time (NOT WeakSet-branded): `op-pack composition failed: duplicate op "{name}"
  declared by more than one pack.` / `op-pack composition failed: op "{name}" collides with a
  reserved handle verb`.

---

## S-000: Walking Skeleton — Contained-Invoke, Kit-Internal Modules, Fail-Closed Mechanism

**Scope**: walking-skeleton · **Dimension**: — · **Requires**: nothing
**Covers**: REQ-DG-06.1–06.5, REQ-DG-07.3, REQ-TSD-01.2 (regression, existing golden re-verified
through new plumbing), REQ-TSD-04.1 (regression) — plus no-REQ debt riders 139 (ast.ts sweep),
141-half (`Session.isPending`), 145 (closure-ref clear), F2 fitness guard.
**Test layers**: unit + integration + one e2e smoke

**Acceptance**: GIVEN the existing `addImport` chain WHEN it flushes THEN the golden output is
byte-identical to today (no behavior regression from the `#invokeContained` rewrite); GIVEN a
sync-throwing test-fixture op WHEN run via `runOp` THEN it's contained with the pinned prefix;
GIVEN a `dialectError`-throwing test-fixture op WHEN run via `runOp` THEN the message passes
through byte-exact, never double-wrapped; GIVEN a rejected run WHEN a further op chains on a
DIFFERENT handle THEN it never executes — surfaces the original rejection via the poison flag.

### Tasks
- [ ] `src/core/dialect-error.ts` (Create) — `dialectError(tail): Error` factory + `isContained
  (err): boolean`, module-private `WeakSet<Error>` brand
- [ ] `src/core/deep-equal.ts` (Create) — kit-internal `deepEqual`, no barrel/subpath
- [ ] `src/core/dialect-handle.ts` (Modify) — shared `#invokeContained(fn, foreignTail)` used by
  BOTH `runRaw` and `runOp` (`runOp` gains `opName` for its tail); mutation-gated `#ensureOpen`
  (register only when print differs from `#lastEmittedText` baseline); row-145: the lazy modify
  directive's memoized getter retains the ts-morph `Project` via its `resolve` closure after the
  getter has resolved (memoized) the live AST — null that `resolve` closure ref immediately
  post-resolution so the `Project`/live AST becomes GC-eligible (one-liner, memory-only fix, no
  observable behavioral change)
- [ ] `src/core/session.ts` (Modify) — `isPending(directive): boolean`
- [ ] `src/core/context.ts` (Modify) — `RunContext.runFailure?: { reason: unknown }`; NO import
  from `dialect-handle.ts` (fitness-guarded)
- [ ] `src/dialects/typescript/ast.ts` (Modify) — row-139: in `parse`'s syntactic-diagnostic
  handling, sweep out the own-property/stack-shape heuristic used to detect/classify ts-morph
  diagnostic errors, replacing it with real ts-morph error tracking (classify off the actual
  diagnostic object `getSyntacticDiagnostics()` reports, not an assumption about the caught
  error's own-enumerable-properties or call-stack shape). Guards REQ-TSD-04.1 (real parse failure
  contained) against a containment path that could misclassify or silently miss a genuine
  ts-morph syntactic error because it only shape-matched a heuristic instead of the real
  diagnostic object (no REQ of its own — hardens TSD-04.1; executor latitude on the exact
  internals)
- [ ] `test/core/dialect-handle.test.ts` (Modify) — REQ-DG-06.1–06.5 (sync/async containment,
  passthrough via synthetic `dialectError` op — constraint 7), REQ-DG-07.3 (same+different
  handle, synthetic reject), mutation-gate zero-directive (generic, via `addImport` idempotent
  no-op), no-reparse parse-count spy; row-145 (closure-ref clear, above) annotated EXPLICITLY
  UNTESTED — memory-only concern, no filler/no-op assertion, do not manufacture a fake assertion
  to claim coverage
- [ ] `test/fixtures/red/**` (Create, partial) — async-op containment red fixture (DG-06.2)
- [ ] `test/dialects/typescript/print-failure.test.ts` (Create) — REQ-TSD-04.2,
  `.raw(sf => sf.forget())` forces a REAL ts-morph print throw
- [ ] Fitness: static import-scan asserting `context.ts` takes no import from
  `dialect-handle.ts` (F2 guard), red-proof via planted fixture
- [ ] `test/fitness/fit-14-*` — VERIFIED: FIT-14 diffs TWO independent halves of
  `pkg-surface-baseline.json` — (a) the `exports`/`dependencies`/`files`/`bin`/`shebang` surface
  (`toEqual` exact match, REQ-FPS-02.1) and (b) the full publishable tarball manifest (`bun pm
  pack --dry-run` vs `baseline.tarball`, new/missing-entry diff, REQ-FPS-02.2) — NOT a single
  export-map check. `deep-equal.ts` is kit-internal (no barrel/subpath): it adds ZERO entries to
  half (a) — `exports`/`dependencies` stay byte-identical, no new export — but DOES add additive
  rows to half (b)'s `tarball` array (it ships inside the `files: ["dist"]` glob regardless of
  export status): `dist/core/deep-equal.js`, `dist/core/deep-equal.d.ts`,
  `dist/core/deep-equal.d.ts.map`. Update `baseline.tarball` only; leave `baseline.exports`/
  `baseline.dependencies` untouched

---

## S-001: `addFunction` ± export — the Named Pain, Typed

**Scope**: happy-path · **Dimension**: P (Path — a new typed declaration-insert flow)
**Covers**: REQ-TSD-09.1–09.8, REQ-DG-07.2 · **Requires**: S-000
**Test layers**: unit + integration

**Acceptance**: GIVEN an empty file WHEN `find(path).addFunction("hi", "(): void {}", {export:
true})` flushes THEN the printed content is byte-exact against a committed golden
(`export function hi(): void {}`); GIVEN a seeded `const foo = 1;` WHEN `addFunction("foo", …)`
runs THEN it REJECTS with the pinned collision message, fail-closed run-wide.

### Tasks
- [ ] `src/dialects/typescript/ops.ts` (Modify) — `addFunction`; local `assertNoCollision(ast,
  name)` (value-namespace + import bindings, cross-kind; `type`/`interface` exempt)
- [ ] `src/dialects/typescript/index.ts` (Modify) — compose `addFunction` into the pack
- [ ] `test/dialects/typescript/ops-declarations.test.ts` (Create) — 09.1–09.8 (golden pairs,
  cross-kind + import-binding collision, type-alias non-collision, CRLF, comment-only, run-twice)
- [ ] `test/dialects/typescript/goldens/**` (Create, partial) — `addFunction` fixtures
- [ ] `test/core/dialect-handle.test.ts` (Modify) — REQ-DG-07.2 (collision reject fail-closed,
  zero batches across the run)
- [ ] `test/fitness/fit-04-*` + `fit-14-*` (Modify) — `addFunction` signature, same slice
- [ ] `docs/authoring-a-dialect.md` (Modify, partial) — `addFunction` paragraph, braces-INCLUDED
  `source` teaching, `@example` cross-referencing `addClass`'s braces-EXCLUDED contrast

---

## S-002: `removeImport` + Row-136 Fail-Loud Coherence + Registry/Docs

**Scope**: happy-path · **Dimension**: P (Path — destructive-edit flow + the modify-vs-open-op
conflict flow, both land in the SAME dialect-handle.ts reject arc)
**Covers**: REQ-TSD-08.1–08.6, REQ-MC-08.1–08.4, REQ-DG-07.1
**Requires**: S-000 (parallelizable with S-001; touches the same test file, sequence after S-001
recommended to avoid merge friction)
**Test layers**: unit + integration

**Acceptance**: GIVEN `import { a, b } from "m";` WHEN `removeImport("a", "m")` flushes THEN `b`
survives byte-exact; GIVEN an absent import WHEN it's the only op THEN zero directives emit;
GIVEN an open `addImport` WHEN `.modify()` is called on the SAME handle THEN it REJECTS (never
silently drops the AST edit) — `.read()` remains the documented escape.

### Tasks
- [ ] [characterization] `test/core/dialect-handle.test.ts` (Modify) — COMMIT 1 of 2, its OWN
  commit (the evidence artefact): the GREEN characterisation test pinning today's silent LWW —
  two directives, in order, raw wins, final tree = raw content. This commit stands alone; it is
  NOT squashed with the reject implementation
- [ ] `src/core/dialect-handle.ts` (Modify) — COMMIT 2 of 2 (implementation commit): `runModify`
  rejects when `#openDirective !== undefined && session.isPending(#openDirective)`. THIS commit
  REPLACES the characterisation test committed in commit 1 with the reject scenario (RED→GREEN
  within this same commit) — the replacement is visible in this commit's diff, never dropped
  silently (constraint 2). Same SLICE (S-002), two commits
- [ ] `src/dialects/typescript/ops.ts` (Modify) — `removeImport(name, from)`: sibling survives,
  last-binding deletes statement, alias matched by exported name, absent = no-op
- [ ] `src/dialects/typescript/index.ts` (Modify) — compose `removeImport`
- [ ] `test/dialects/typescript/ops-removeImport.test.ts` (Create) — 08.1/08.2/08.3/08.5
  (dryRun preview via `pendingSnapshot()`)
- [ ] `test/core/dialect-handle.test.ts` (Modify) — REQ-TSD-08.4 (zero directives), 08.6 (RYOW,
  add-then-remove same chain = one modify = byte-identical seed), REQ-MC-08.1–08.4, REQ-DG-07.1
  (row-136 fail-closed, zero batches across BOTH handles), mixed old+new-op coalescing fixture
  (REQ-MC-01 coverage note, non-normative — `addImport`+`addFunction`+`removeImport` one chain)
- [ ] `test/core/dialect-handle.test.ts` (Modify) — row-141 kept-half, batch-grouping component
  (no REQ of its own; pending-changes row 141 / spec.md's row-141 note: "kit-internal,
  test-authoring hygiene"): annotate the mixed old+new-op coalescing fixture above with an
  explicit `// proves REQ-MC-01.2 (two independent handles on different paths batch into two
  separate modify directives, no cross-contamination) and REQ-TSD-03.1 (a create-then-addImport
  chain in one run coalesces to exactly one modify at flush)` comment naming both existing,
  already-tested scenarios it demonstrates — an ANNOTATION (both scenarios are proven elsewhere
  already; this is traceability, not a new assertion)
- [ ] `test/dialects/typescript/goldens/**` (Create, partial) — `removeImport` fixtures
- [ ] `test/docs/security-authoring-guard.test.ts` (Modify) — extend the positive shipped-verbs
  loop with `removeImport` (this slice's own op) AND `addFunction` (already shipped by preceding
  S-001 — safe to name now, since S-001 lands before S-002 in Build Order); `addVariable`/
  `addClass` are EXCLUDED here — they ship later in S-003 and naming them now would violate
  REQ-DAS-01 (the loop names exactly the shipped API); their loop/doc entries move to S-003, the
  slice that ships them (see S-003 Tasks). DELETE the negative unshipped-surface test (lines
  76-78); assert `sensitive-areas.md` `security (code execution)` row reads `high`; assert the
  stale "anticipated"/"confidence: low" sentence is ABSENT
- [ ] `openspec/sensitive-areas.md` (Modify) — promote medium→high; fix stale paragraph
- [ ] `test/fitness/fit-04-*` + `fit-14-*` (Modify) — `removeImport` signature, same slice
- [ ] `docs/authoring-a-dialect.md` (Modify, partial) — op-inventory prose update ("one
  structured op" claim now false), `removeImport` paragraph, row-136 reject rule + `.read()`
  escape, async-op containment note

---

## S-003: `addVariable` + `addClass` ± export — Generality Demo, Cuttable

**Scope**: happy-path · **Dimension**: D (Data — same insert-mechanism, different declaration
kinds: const/let/var vs class)
**Covers**: REQ-TSD-10.1–10.4, REQ-TSD-11.1–11.4, REQ-TSD-01.1 · **Requires**: S-001 (reuses
`assertNoCollision`)
**Test layers**: unit + integration
**Cuttable**: YES — owner-authorised, no re-ask. `test/dialects/typescript/ops-exact-set.test.ts`
(the REQ-TSD-01.1 exact-set gate) SURVIVES every cut — REQ-TSD-01.1 is a signed MODIFIED REQ and
is NOT cuttable; on cut, this test's allow-list array is EDITED (not deleted) to the reduced
three-op set `["addFunction","addImport","removeImport"]` (sorted, per the test's own
convention). The cut DELETES only: `test/dialects/typescript/ops-declarations-cuttable.test.ts`,
the `addVariable`/`addClass` golden fixtures, their FIT-04/FIT-14 baseline rows, their
guard-loop + doc names (S-003-owned per constraint 6 / Tasks above), and their JSDoc/`ops.ts`
implementations — all in ONE atomic commit. Because S-002's guard-loop/doc entries name only
`removeImport`+`addFunction`, the cut lever automatically covers just S-003's own entries;
S-002's landed guard/doc work stays green regardless of the cut decision.

**Default**: BUILD. S-003 is executed as planned; the cut lever fires ONLY on an OBSERVABLE
stop-condition, checked before S-003 completes: the executor HALTS and SURFACES (never decides
the cut itself) when EITHER (a) cumulative NON-TEST source diff for this change exceeds ~1,200
added/modified lines — the top of the L lines-estimate band (`triage.md` Criteria Evaluation row
2, "Lines affected (estimated) | ~600-1200 | L") — OR (b) 6+ NEW ADRs exist for this change (the
standing tripwire).
**Authority**: on either trigger, the ORCHESTRATOR — never the executor — invokes the cut, under
the standing owner pre-authorisation (owner rulings #2/#3 — "cut FIRST, no re-ask"); the owner is
NOT re-asked. Executor watches and surfaces; orchestrator invokes. On cut: the executor performs
exactly the delete-vs-edit split specified in the **Cuttable** line above, in ONE atomic commit,
and the deferral is registered as a pending-changes row at archive.

**Acceptance**: GIVEN an empty file WHEN `addVariable("PI", "3.14159")` flushes THEN golden
`const PI = 3.14159;`; GIVEN `addClass("Point", "  x = 0;", {export: true})` WHEN it flushes
THEN golden begins `export class Point`; GIVEN `Object.keys(dialect.ops)` sorted WHEN compared
THEN it `toEqual`s the exact shipped set (five ops if not cut, three if cut).

### Tasks
- [ ] `src/dialects/typescript/ops.ts` (Modify) — `addVariable`, `addClass` (reuse
  `assertNoCollision`)
- [ ] `src/dialects/typescript/index.ts` (Modify) — compose both; finalize widened op-type/`find`
  return type for the shipped set
- [ ] `test/dialects/typescript/ops-declarations-cuttable.test.ts` (Create) — 10.1–10.4,
  11.1–11.4 (own collision scenarios, empty-file seeds)
- [ ] `test/dialects/typescript/ops-exact-set.test.ts` (Create) — REQ-TSD-01.1, `toEqual` on the
  sorted allow-list
- [ ] `test/dialects/typescript/goldens/**` (Create, partial) — `addVariable`/`addClass` fixtures
- [ ] `test/fitness/fit-04-*` + `fit-14-*` (Modify) — both op signatures, same slice
- [ ] `test/docs/security-authoring-guard.test.ts` (Modify) — extend the SAME positive
  shipped-verbs loop already extended by S-002 with `addVariable`/`addClass`, completing the
  four-op set in the SAME slice that ships them (REQ-DAS-01: the loop never names an unshipped
  verb) — the cut lever therefore covers only these two entries; S-002's already-landed
  `removeImport`/`addFunction` loop entries are unaffected by a cut
- [ ] `docs/authoring-a-dialect.md` (Modify, partial) — `addVariable`/`addClass` paragraphs

---

## S-004: `withOps` Collision + Reserved-Name Diagnostic

**Scope**: edge-case · **Dimension**: R (Rule — composition-time validation)
**Covers**: REQ-DG-02.1 (restate/confirm unchanged), REQ-DG-02.2–02.5 · **Requires**: S-000
(independent of S-001–S-003; touches `define-dialect.ts`, not `ops.ts`)
**Test layers**: unit + integration (real `SourceFile` fixtures)

**Acceptance**: GIVEN two REAL, disjoint op-packs composed via `withOps` WHEN composed THEN no
diagnostic fires; GIVEN two packs declaring the SAME op name WHEN composed THEN it throws
synchronously naming the op; GIVEN an op-pack declaring `then` WHEN composed THEN it throws
naming the reserved-vocabulary collision.

### Tasks
- [ ] `src/core/define-dialect.ts` (Modify) — `withOps` eager synchronous fail-closed check:
  cross-pack collision + `RESERVED_HANDLE_NAMES` (`then`, `read`, `raw`, `modify`, `rename`,
  `move`, `copy`, `remove`) — throws are NOT WeakSet-branded (outside `dialectError()`)
- [ ] `test/core/define-dialect-collision.test.ts` (Create) — REQ-DG-02.2 (GREEN disjoint real
  packs), 02.3 (`defineOpPack` standalone), 02.4 (RED colliding real packs), 02.5 (RED `then`)
- [ ] `test/fixtures/red/**` (Create, partial) — colliding op-pack pair typed over real
  `SourceFile`, outside `src/conformance/**`
- [ ] `test/types/define-dialect.test.ts` (Modify) — note REQ-DG-02.1 unchanged; runtime
  collision is now the load-bearing proof
- [ ] `docs/authoring-a-dialect.md` (Modify, partial) — `withOps` collision + reserved-names line

---

## S-005: Conformance Tail — Adversarial Samples, Leaf Rule, Real-Base Probe

**Scope**: edge-case · **Dimension**: R (Rule — kit-wide "what counts as a valid dialect")
**Covers**: REQ-DC-06.1, REQ-DC-06.2, REQ-DC-07.1, REQ-DC-08.1 · **Requires**: S-003 (runs
against the FINAL shipped op-set, post cut-lever decision)
**Test layers**: integration (conformance harness)

**Acceptance**: GIVEN a minimal fixture supplying none of the six adversarial shapes WHEN
`testDialect` runs THEN all six still execute; GIVEN a fixture with identity `parse`/`print`
WHEN `testDialect` runs THEN it FAILS before the round-trip assertion vacuously passes; GIVEN the
shipped TypeScript dialect WHEN the PRE-EXISTING FIT-01 import-graph walk runs (`test/fitness/
fit-01-*`, unmodified by this slice) THEN it reports no cross-dialect/AST-library import — this
FIT-01 walk IS REQ-DC-07.1's proof mechanism (design ADR-0012 amendment, DOCUMENTED-LIMIT: no
new import-graph scanner is added inside `src/conformance/**`; the kit DOCUMENTS the leaf
contract, third-party authors self-run their own static check).

### Tasks
- [ ] `src/conformance/index.ts` (Modify) — inject six mandatory adversarial samples
  (empty/comment-only/4 MiB/CRLF/BOM/duplicate-target), additive, no opt-out field; real-base
  structural probe (`parse(sample)` returns non-null, distinct from input string). NO leaf-rule
  scanning logic is added here — DOCUMENTED-LIMIT (design ADR-0012 amendment clause 3, north-star
  CQ-B): `src/conformance/**` gains no import-graph scanner of any kind; REQ-DC-07.1's proof for
  the shipped TypeScript dialect is the PRE-EXISTING FIT-01 import-graph walk (unmodified by this
  slice), never new code in the conformance module
- [ ] `src/conformance/index.ts` + `src/testing/contract-fake.ts` (Modify) — both import
  `deepEqual` from `../core/deep-equal.ts`; drop the duplicated local copy
- [ ] `test/conformance/typescript-conformance.test.ts` (Modify) — REQ-DC-06.1 (six samples
  execute); REQ-DC-07.1 surfaced as a documentation-pointer assertion ONLY (asserts the FIT-01
  fitness function is the load-bearing proof for the shipped dialect — no duplicate import-graph
  scan authored here or anywhere under `src/conformance/**`)
- [ ] `test/conformance/planted/**` (Create/Modify) — identity-fixture RED (REQ-DC-08.1);
  opt-out-attempt RED/compile-pin (REQ-DC-06.2 — fixture type has no disabling field)
- [ ] `docs/authoring-a-dialect.md` (Modify, partial) — leaf-rule DOCUMENTED-LIMIT statement:
  (a) the kit DOCUMENTS the leaf contract (a dialect's `parse`/`print`/ops MUST NOT import
  another dialect or a foreign AST library) in this recipe doc, stating explicitly that
  third-party dialect authors are expected to self-run their OWN static check (e.g. an
  import-graph lint) to enforce it locally — the kit ships no runtime/static scanner for it;
  (b) states that the SDK's own shipped TypeScript dialect's proof is the PRE-EXISTING FIT-01
  import-graph walk in the test suite, not a conformance-module check (per owner CQ-B
  affirmation condition — never hidden)

---

## Build Order

| Order | Slice | Note |
|---|---|---|
| 1 | S-000 | skeleton, blocks all others |
| 2 | S-001 | addFunction — named pain, earliest possible per constraint 4 |
| 3 | S-002 | removeImport + row-136 + registry — sequence after S-001 (shared test file) |
| 4 | S-003 | addVariable/addClass — requires S-001's `assertNoCollision`; cut-lever gate here |
| 5 | S-004 | withOps collision — independent, parallelizable with S-001–S-003 after S-000 |
| 6 | S-005 | conformance tail — requires S-003 (final op-set + cut-lever decision settled) |

## Coverage Check

**REQ-IDs** (47/47, no orphans): TSD-01.1→S-003 (gate test SURVIVES a cut — allow-list array is
EDITED to the reduced set, not deleted; see S-003 Cuttable), TSD-01.2→S-000(regression) ·
TSD-04.1→S-000(regression), TSD-04.2→S-000 · TSD-08.1–08.6→S-002 · TSD-09.1–09.8→S-001 · TSD-10.1–10.4,
TSD-11.1–11.4→S-003 · DG-02.1→S-004(restate), DG-02.2–02.5→S-004 · DG-06.1–06.5→S-000 ·
DG-07.1→S-002, DG-07.2→S-001, DG-07.3→S-000 · MC-08.1–08.4→S-002 · DC-06.1,06.2,07.1,08.1→S-005.
Verify-final obligations: sensitive-areas promotion + stale-paragraph→S-002;
addFunction/addClass `@example` braces contrast→S-001(addFunction half)+S-003(addClass half).

**File Changes rows** (28/28, every row owned — multi-slice touches listed per-slice above, not
orphaned): `dialect-error.ts`/`deep-equal.ts`/`context.ts`/`session.ts`/`ast.ts`(139)/
print-failure.test.ts/fit-14 deep-equal row→S-000 · `dialect-handle.ts` (invokeContained,
mutation-gate, row-145, poison flag)→S-000, (row-136 guard)→S-002 · `ops.ts`/`index.ts`→S-001+
S-002+S-003 (incremental) · `define-dialect.ts`→S-004 · `conformance/index.ts` +
`testing/contract-fake.ts`→S-005 · all per-op test/golden/FIT-04/FIT-14 files→their op's slice ·
`dialect-handle.test.ts`→S-000+S-001+S-002 (per-REQ, listed above) · `define-dialect-collision.
test.ts`/`types/define-dialect.test.ts`→S-004 · `typescript-conformance.test.ts`/`planted/**`→
S-005 · `test/fixtures/red/**`→S-000(async)+S-004(collision) · `security-authoring-guard.test.
ts`→S-002+S-003 (incremental: `removeImport`+`addFunction` loop entries in S-002,
`addVariable`+`addClass` loop entries in S-003) · `sensitive-areas.md`→S-002 ·
`docs/authoring-a-dialect.md`→S-001..S-005 (incremental sections, no single-owner claim).

## Anti-Pattern Check

No horizontal/layer-named slices — each op slice is one cohesive user-visible capability, S-004/
S-005 are cohesive validation rules, not layer grabs. Every slice cites ≥1 REQ-ID. No two-SPIDR-
dimension slice (S-002 bundles two FLOWS under one Path dimension, not two dimensions). 6 total
(target 4-7 ✓; 5 excluding skeleton, above the ≥3 floor). No slice depends on >2 others (max is
1 explicit dependency beyond the implicit S-000).

## Droppable Analysis

**S-003 only** (owner-authorised cut lever, REQ-TSD-10/11). Nothing else is droppable: the
reckoning bar (triage Owner Ruling #3) pins `addFunction`±export (S-001) and generality-proven
(S-004 collision diagnostic + S-005 conformance tail) as NON-cuttable — even if S-003 cuts, the
committed-next obligation is still discharged. S-002 (removeImport) is a named-pain edit and a
judgment-day-flagged correctness fix (row-136) — not proposal-level polish.

## Risks

- S-002 bundles two logically distinct flows (removeImport op + row-136 reject) under one Path
  dimension — accepted because design's own File Changes table ties them to the SAME
  `dialect-handle.test.ts` reject arc and the SAME doc/registry slice (constraints 2, 6, 9);
  flagged for plan-verify to confirm this reads as one cohesive slice, not a merge-of-convenience.
- S-000 carries ~11 tasks — above the skill's "≤8 micro-tasks" heuristic — justified as one
  indivisible mechanism (contained-invoke + fail-closed poison flag + mutation-gate are one seam,
  mirrors stage-5's own accepted S-001 precedent).
- `docs/authoring-a-dialect.md` is edited incrementally across all 5 SPIDR slices — risk of
  merge-order drift if slices land out of order; mitigated by the fixed Build Order above.

## Upstream Publication

N/A — `spec_source: internal`, Step 8b skipped.
