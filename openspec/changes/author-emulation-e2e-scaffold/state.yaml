# Orchestrator-owned DAG state — author-emulation-e2e-scaffold
change: author-emulation-e2e-scaffold
triage: L
sensitivity_override: false
completed_phases:
  - triage
  - explore
  - propose
  - spec
  - design
current_phase: design
current_phase_status: complete — proposal.md + obs #942 (executor) + blind BA/PM synthesized. 8 capabilities (contract with spec). Landed-API verified on origin/feat/schematic-local-files — scaffold({from,to,options,include,exclude,rename,force}), copyIn 7th wire op, create({templateFile}) overload. KEY findings: walk order IS sorted in code (walk.ts readdirSync().sort()) but NOT spec-pinned → corpus declares intra-walk-order NORMATIVE + risk reported; FIT-22 taken upstream → ours renumber FIT-23..26; e2e filename = author-emulation-scaffold.e2e.test.ts (collision confirmed, upstream owns scaffold.e2e.test.ts). Council synthesis: PM's corpus-droppable concern DISSOLVED by D1 same-file identity (corpus = golden, marginal cost ≈0; droppable = extra matrix rows instead); BA coverage manifest TWO LEDGERS (exercised REQ-map + NOT-exercised honest gaps: wiring/tsconfig-AST/rendering) adopted as gap-honesty success criterion (D2 mechanical check); PM gate ruling = scaffold-scenario slices gated on judgment-day-passed + MERGED-TO-MAIN (not archive) + signature-shift tripwire; matrix boundedness rule = every row cites signed REQ-ID or owner boundary, count pinned in spec; zombie tripwires = (1) any capture/report code branching on non-scaffold family → re-triage, (2) scenario without new REQ-ID citation → spec unfreeze required. BA pending-changes row for D3 token gap drafted (PC-SPEC-FSC-TOKENS, registered at build). No problem-drift.
in_loop_iteration: 0
current_phase_design_status: complete — design.md + obs #944. architecture_impact ADDITIVE (0 baseline deviations — corpus = new member of golden-idiom family; refines explore's third-idiom `deviates` → `aligns`). ADRs 0047 (corpus authority normative-core/informative-shell), 0048 (third golden idiom + single-capture-path + no-self-heal + runFactoryForTest seam), 0049 (canonical serialization binds reader) — inline drafts in design.md. Council rulings R-A (unfreeze seam), R-B (s-00 no report), R-C (family prefixes) adopted in-doc. Canonical key order DELIVERED §4.3; outcome placed INSIDE normative (reasoned deviation from council envelope — binding region = one subtree). Test derivation: 39/39 REQs (canonical file-derived count; spec envelope's "42" was envelope arithmetic error, artifacts are truth). Build-gate split: UNGATED walking skeleton = s-00 + capture/format/report modules + FIT-23..27 + .gitignore/pending-changes; GATED = M-01..M-21 on schematic-local-files merge + apply-time re-verify. ~25 file rows (23 create/2 modify).
steward_foresight: ALIGNED (obs #945 + north-star.md, 2026-07-13); 3 CQs owner-answered 2026-07-13 — CQ-1 self-reported FRICTION ledger sufficient for change #1 (harder mechanism re-evaluated at family #2), CQ-2 visibility-not-validation boundary consciously ACCEPTED (corpus v0/PROVISIONAL until PC-PROTO-01 — reckoning bar set accordingly), CQ-3 full infra-hardening NOW (anti-tautology protects the load-bearing property; families inherit free). Reckoning hooks carried: FRICTION="none observed" must not pass vacuously without owner nod; GREEN ≠ engine-honored; infra-only green ≠ delivered (21/22 records build-gated); README must teach scaffold→create/copyIn lowering (steward journey-sim leak — slice/apply must place it).
next_phase: slice (sonnet executor; 4-7 SPIDR; walking skeleton = UNGATED infra spine incl. s-00; M-01..M-21 slices carry the merge gate + signature tripwire; a slice owns PC-SPEC-FSC-TOKENS registration; droppable = extra matrix rows never the corpus; NO Step 8b publish — spec_source internal) → verify --mode=plan (orchestrator-executed, 2 blind judges opus, max 3 iters)
execution_mode: interactive
artifact_store: hybrid
spec_status: V2 SIGNED (owner 2026-07-13, 42 REQs/21-row matrix/FIT-23..27; micro-unfreeze 1 owner-approved 2026-07-13 — REQ-ITC-01 wrap target corrected makeSpyClient → runFactoryForTest, architect P1 orchestrator-verified factual citation error: runFactoryForTest inlines its own RecordingClient and alone exposes tree+error needed by GCC-09/M-21) — V1 (34 REQs, 20-row matrix) council-reviewed blind (BA needs-V2 1 MAJOR; QA needs-V2 2 BLOCKER; TW needs-V2 2 BLOCKER, 1 downgraded by orchestrator verification: DryRunEntry.kind EXISTS on origin/feat/schematic-local-files plan.ts:71). V2 = 42 REQs / 6 domains / 21-row matrix (M-21 cross-chunk atomicity added) / FIT-23..27 (FIT-27 = mechanized anti-tautology no-corpus-write scan). New REQs: AEG-06 (DX-friction capture — BA Intent pillar), AEG-07 (git-hostile fixtures setup-materialized), GCC-09 (NORMATIVE rejection record {reason,verb,path}, message text excluded), GCC-10 (naming: test/e2e/author-emulation/corpus/, <matrix-id>.<slug>.transcript.json, coverage-manifest.md, README.md), GCC-11 (README 5 sections + in-file normative/informative self-labeling), GCC-12 (skeleton corpus vs existing 6 ops — gate-decoupled RED-provability), RPT-05 (report content: verb+path+kind, seam disclaimer, gap notice), SCM-05 (rejection rows assert full triple). All 34 V1 IDs stable; disposition table in executor envelope; zero rejected findings. Carried to design/verify-plan: GCC-06 canonical key-order exact listing is design-ratified (verify-plan confirms design delivers it); M-19 symlink CI-target coverage note.
notes: >
  Problem: pre-engine-return trust hardening — 6/7 existing e2e are surgical fake-based paths;
  no realistic author-emulation exists; IR contract invisible outside asserts. Scope: scaffold
  mutation family ONLY + shared IR-capture/report infra (3 audiences: committed golden
  baselines, engine-handoff corpus, human-readable report).
  Owner requirements registered (Engram obs #937): R1 per-run report output gitignored;
  R2 idempotent/deterministic log filename (re-run overwrites) — inject verbatim at
  propose/spec.
  Council carried flags for explore/spec/design:
  (1) BUILD gated on schematic-local-files archiving — scaffold/copyIn/create({templateFile})
  verified ABSENT from this worktree's src/; planning ungated (spec V3 signed, PR #20 merged).
  Slices must split: infra spine buildable NOW against wire.ts + makeSpyClient (ungated);
  scaffold scenarios gated on merge + apply-time re-verify against the LANDED API.
  (2) Reuse, don't rebuild: seam capture EXISTS — test/support/spy-client.ts (makeSpyClient)
  + golden.ts loader + two golden idioms in test/golden-ir/. Corpus must reconcile as the
  third idiom or be justified (ADR).
  (3) ADR candidates: corpus AUTHORITY (normative vs illustrative — blocking for design);
  format/location derived from family-agnostic wire.ts schema, NOT scaffold-shaped; corpus
  is v0/PROVISIONAL (its consumer, the real engine, doesn't exist until PC-PROTO-01 — do not
  over-polish); by-reference capture boundary (directive reference only, never bytes —
  REQ-BRC-04).
  (4) Spec MUST pin the scenario matrix as a FIXED ENUMERATED table, each row citing a signed
  schematic-local-files REQ or an in-scope boundary bullet (REQ-FSC-05 naming pipeline,
  REQ-FSC-04.2, REQ-FSC-08, REQ-BRC-05/06, batch-cap REQ-04.2/04.3, REQ-FEH-02, REQ-CCL-06)
  — otherwise the change never finishes.
  (5) Spec wart: by-reference op string deliberately UNPINNED in schematic-local-files spec —
  spec asserts STRUCTURALLY; literal golden bytes are build-derived post-landing, never
  hand-frozen from spec text.
  (6) Fitness function candidates: corpus byte-determinism; no-binary-bytes-in-corpus guard;
  single-capture-path guard (EngineClient seam via makeSpyClient only).
  (7) PM sequencing: planning parallel is fine; /build capacity decision must confirm this
  FOLLOWS stage-6, not displaces it. Scope-creep tripwire: absorbing other mutation families.
  (8) Walking skeleton = infra spine + simplest full-generator scenario; boundary rows are
  vertical slices. Do NOT carve a separate infra-only change.
  (9) File-count is fixture-boilerplate-heavy (~20-30) — explore re-triages if true footprint
  is XL-shaped.
  (10) Operational: GateGuard false-positive on this change's paths ("auth" substring in
  "author-emulation") — every first write needs the 4-fact justification.
updated_at: 2026-07-13
