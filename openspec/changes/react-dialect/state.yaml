# Orchestrator-owned DAG state — react-dialect
change: react-dialect
triage: L
execution_mode: automatic
completed_phases:
  - triage
  - explore
  - propose
  - spec
  - design
  - steward-foresight
  - slice
  - plan-verify
current_phase: spec-awaiting-sign
current_phase_status: >
  SPEC V5 MINTED 2026-07-17, UNSIGNED — awaiting OWNER sign. This is a HUMAN
  gate and it blocks everything downstream (sdd-apply halts `spec-not-signed`
  against an unsigned spec). V5 was unfrozen from V4-SIGNED to correct the
  spec defects the Council found; all 14 V4 REQ-IDs are preserved, none
  removed or reused. AMENDED: REQ-RXD-05 (merge-or-create made shape-aware —
  defines a specifier's LOCAL NAME and scopes the merge target strictly to
  non-type-only named clauses; ONE idea closes all four correctness bugs;
  scenarios 05.1-05.4 keep their expected outcomes, they were the correct
  baseline), REQ-RXD-06 (adds an IMPORT_RESERVED_WORDS denylist — 46
  ECMAScript reserved/strict-mode words + await, a real behaviour change; and
  removes the false "full prototype-walk set" claim in favour of the accurate
  Set-key-safety rationale, wording only), REQ-RXD-11 (11.5's literal example
  "{" was unimplementable under ADR-03 → corrected to the delimiter-balanced
  "{1+}"; closes a long-standing carry-forward). MINTED: REQ-RXD-15
  (contributor docs must teach validatedOp — closes ARCH-1). New scenarios
  under existing REQs: 05.5-05.10, 06.7, 06.8, 11.6.
verify_final_status: >
  FAILED 2026-07-17 at HEAD f12ffba — see verify-report.md (verdict `fail`,
  adversarial_review `required`) and council-findings.md.
legacy_verify_fail_note: >
  VERIFY FINAL **FAILED** 2026-07-17 at HEAD f12ffba — see verify-report.md
  (verdict `fail`, adversarial_review `required`) and council-findings.md.
  The BUILD is intact and the suite is genuinely green; the GATE is what
  failed. sdd-verify --mode=final gated on ONE item (F-01, MAJOR/quality).
  The four BLIND anti-anchored Council personas run in parallel with it
  (architect, qa-engineer, security-engineer, tech-writer — all opus) found
  substantially more, including FOUR REAL CORRECTNESS BUGS in addImport that
  the verifier missed. The fan-out is what caught them: verify-final alone
  called this "a one-line JSDoc fix, not a re-plan" and would have shipped
  it. ROUTED to Planner -> sdd-spec (unfreeze=true; V4 is SIGNED) -> apply ->
  re-verify final -> judgment-day. NOTE: this file sat frozen at a stale
  `build-complete` for ~1h while a recurring macOS TCC fault cut off all
  ~/Documents access before the FAIL could be persisted; corrected on resume.
build_status: >
  All six slices S-000..S-005 APPLIED and committed. Both in-loop loops
  exited clean (verify-in-loop-5.md closed S-002 at iteration 5;
  verify-in-loop-6.md closed the S-003+S-004+S-005 final batch at iteration
  6). Final batch commits: cf89a52, 548c581, 616ff75, 0257e5c, f78cb88.
  Design contract confirmed by `git diff --name-status 6ee9af0..f78cb88`:
  ZERO src/** changes in that batch. Suite: 1797 pass / 0 fail in a clean
  /private/tmp worktree (the ~/Documents number lies — see carry_forward).
  Retained for history: this is what the gate then found wanting.
legacy_build_complete_note: >
  BUILD COMPLETE 2026-07-17. All six slices (S-000..S-005) APPLIED and
  VERIFIED DONE; both in-loop loops exited clean — verify-in-loop-5.md closed
  S-002 at iteration 5, verify-in-loop-6.md closed the S-003+S-004+S-005
  final batch at iteration 6 (PASS). Final batch commits: cf89a52 (S-003
  conformance corpus), 548c581 (S-004 authoring docs), 616ff75 (S-005
  installed-consumer six-subpath parity), 0257e5c + f78cb88 (apply records).
  Design contract confirmed by `git diff --name-status 6ee9af0..f78cb88`:
  ZERO src/** changes — every touched file matches its Create/Modify action
  in design.md §4.2. Evidence, all independently REPRODUCED by the verifier
  rather than trusted from the apply record: scoped batch 245 pass / 0 fail;
  full suite 1797 pass / 0 fail across 2 runs in a clean /private/tmp
  worktree of f78cb88 (1779 baseline + 18 net new); all 4 mutation probes
  reproduced with their exact claimed isolation; S-004's worked example
  executed byte-exact against real parse/print/setJsxProp/addImport. TDD call
  on this batch: the mutation-probe-for-RED substitution is judged ADEQUATE
  and appropriately narrow — used only where behaviour genuinely pre-existed,
  since a literal RED there would require breaking working production code;
  S-004 had genuine RED available and used it, which is the stronger choice
  when it exists.
in_loop_iteration: 0
slices_done: [S-000, S-001, S-002, S-003, S-004, S-005]
next_phase: >
  OWNER SIGN of spec V5 — a HUMAN gate, blocking. Then sdd-apply against V5:
  (a) the `getImportDeclaration` predicate, per REQ-RXD-05's shape-aware
  algorithm — root cause of all four correctness bugs; (b) SEC-1 reserved
  words per REQ-RXD-06; (c) F-01/DOC-1 the false `@example` at
  src/dialects/react/index.ts:49-56 plus a .d.ts baseline regen; (d) QA-5 the
  assertion leak (`toContain("name")` matches `"propName"`, so the hostile
  battery cannot tell WHICH validator is wired); (e) DOC-2 the .tsx-only doc
  gap; (f) DOC-4 doc-index rot at docs/README.md:16 and
  docs/quickstart.md:186; (g) ARCH-3 ADR-02's false "by construction" clause
  vs design §4.4; (h) REQ-RXD-15's validatedOp contributor doc. THEN
  re-verify final -> judgment-day (BLIND; required: L + two sensitive areas)
  -> steward reckoning -> architecture audit gate -> archive.
  FIX-AGENT NOTE: adopt the Council's mutation-probe amendment — a probe that
  substitutes for RED MUST include at least one NEAR-MISS mutant, not only an
  absence mutant. QA-5 exists precisely because every probe so far was an
  absence mutant, and absence mutants are the ones a substring-weak assertion
  still catches.
archive_commitments:
  - >
    DOC-3 — `could not parse "X" as TypeScript` hardcoded in the
    dialect-GENERIC path at src/core/dialect-handle.ts:178. sdd-spec ruled it
    a PENDING-CHANGE, not this change's spec: the file is marked
    Read-only/zero-change in this change's own signed design §4.2, and a
    correct fix means threading a per-dialect display name through the
    `dialect-handle`/`define-dialect` seam every dialect shares — which is
    the "any change to the engine" that triage.md puts out of scope. Note
    test/core/dialect-handle.test.ts:328 currently PINS the wrong text.
  - >
    ARCH-2 — src/core/jsx-name-validator.ts holds JSX grammars in a
    file-type-agnostic layer with exactly one consumer. Architect's call:
    NOT a rebuild. Extend ADR-01's already-registered peer-module followup —
    when dialect #3 lands, split it: `validatedOp` + `reject-tail` stay in
    core (genuinely generic), the JSX grammars move to the leaf.
  - >
    Subprocess-timeout debt (pre-existing, out of scope) — see carry_forward.
  - >
    The .tsx/.jsx pending-changes row must be folded-or-reaffirmed at
    archive, never duplicated.
artefacts:
  triage: openspec/changes/react-dialect/triage.md (obs #1032)
  explore: openspec/changes/react-dialect/explore.md (obs #1035; AST decision obs #1037 owner-ratified ts-morph ScriptKind.Tsx)
  proposal: openspec/changes/react-dialect/proposal.md rev 2 (obs #1041; op pair setJsxProp + addImport council-ratified; erratum notes on lines 19/133)
  spec: specs/react-dialect/spec.md V4 SIGNED + specs/local-consumption/spec.md V2 (obs #1045; V3 minted+retired; V4 = extensionless-reject + denylist {__proto__, constructor, prototype})
  design: openspec/changes/react-dialect/design.md rev 2 (obs #1051; additive; ADRs 01-05; spike 6 legs PASS; File Changes 16C/9M/3R)
  north_star: openspec/changes/react-dialect/north-star.md (obs #1055; foresight ALIGNED; CQ-1/2/3 owner-affirmed 2026-07-16)
  slices: openspec/changes/react-dialect/slices.md rev 2 (obs #1056; S-000..S-005 self-contained build brief; all-or-nothing completion)
  plan_verify: verify-plan-1.md (gaps, obs #1057) + verify-plan-2.md (READY, obs #1060)
  apply_progress: openspec/changes/react-dialect/apply-progress.md
  verify_in_loop: verify-in-loop-1.md (S-000) + verify-in-loop-2.md (S-001, needs-fix) + verify-in-loop-3.md (S-001, pass) + verify-in-loop-4.md (S-002, needs-fix) + verify-in-loop-5.md (S-002, PASS) + verify-in-loop-6.md (S-003+S-004+S-005 final batch, PASS)
  verify_final: openspec/changes/react-dialect/verify-report.md (verdict fail, adversarial_review required; gates on F-01 ONLY — the Council's findings are NOT in it)
  council_findings: openspec/changes/react-dialect/council-findings.md (engram obs #2288) — 4 real addImport correctness bugs + 2 spec defects the final verifier missed; blind parallel fan-out (architect, qa-engineer, security-engineer, tech-writer, all opus)
carry_forward:
  - REQ-RXD-11.5 wording amendment -> pending-changes at archive.
  - >
    Strict TDD ordering violation on S-002 (impl written in the same editing
    pass as the tests; RED proven retroactively) is DISCLOSED, not resolved.
    sdd-verify --mode=final MUST audit it under TDD Cycle Adherence — it must
    not downgrade to clean by attrition.
  - >
    RESOLVED 2026-07-17 by diagnostic. Full-suite instability is
    ENVIRONMENTAL — not a regression, and NOT parallelism. The
    parallel-execution hypothesis is refuted at the root: bun test is
    sequential by default and package.json:64 runs a bare `bun test` with no
    flags, so nothing was ever parallel. Control experiment settles the
    regression question: HEAD's exact code (6ee9af0) checked out into
    /private/tmp yields 1779 pass / 0 fail across 3/3 runs (~23s). Therefore
    apply-progress.md's "1779 passing / 0 failing" claim is truthful and
    exactly reproducible — just not under ~/Documents. Mechanism:
    subprocess-bound tests (bun install / bun link / tsc --noEmit via
    blocking spawnSync) run against bun's default 5000ms timeout, while
    first-touch file scanning under ~/Documents inflates the first
    `tsc --noEmit` from 663ms to 16694ms (26x). tsc --extendedDiagnostics
    proves the work is identical on both sides (256 files / 48335 symbols /
    556 types), so it is not ts-morph, not the type graph, and not the react
    dialect. Strong inference: Microsoft Defender (wdavdaemon) real-time
    scan — NOT A/B-proven, as a corporate EDR cannot be disabled to confirm.
    Ruled out: parallelism, regression, ts-morph/type-graph growth,
    dist/testing/commons d.ts drift, core AST leakage (FIT-37 holds), iCloud
    sync, port binding, process.cwd() mutation. No shared-state defect exists.
  - >
    OPEN — pre-existing test debt, explicitly NOT react-dialect scope:
    subprocess-bound tests declare no explicit timeout
    (test/docs/quickstart-docs.test.ts:312 and :319, the S-03
    permissive-proof CI guard, the installed-consumer-vantage e2e leg).
    Their measured work is ~2s even on a fast filesystem against a 5000ms
    budget — the declared budget does not describe the work, Defender or not.
    Fix = explicit per-test timeouts (~30 min), NOT a raised global timeout
    (that would mask genuinely slow tests everywhere). Environment-level
    companion, not committed: a Defender real-time-scan exclusion for the
    repo, or at minimum for .tmp-*/ scratch dirs.
notes: >
  Owner rulings embedded: .tsx-only (jsx pending-changes row added mid-plan,
  archive must fold-or-reaffirm never duplicate); extensionless paths REJECT
  (forward-compat with future .jsx); denylist of 3. Executor context: slices.md
  self-contained for contracts, repo files are copy-adaptable precedents.
  SDD skill files resolve from /home/barri/Projects/Claude-Code-harness/.claude/skills/.
