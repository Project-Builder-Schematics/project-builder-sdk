# Fixture A — mirrors `openspec/specs/` with a LIVE (non-version-history) retired citation

This capability still normatively depends on `package-root-containment` for its boundary.

The line above carries no history marker of any kind — it is a live, orphaned mention the
sweep LOGIC must flag. [red-fixture] REQ-FTG-06.4 pairing. Never read by the real
`openspec/specs/` sweep (that invocation is archive-sync only, clause (e)); read as TEXT by
fit-43's red-proof only.
