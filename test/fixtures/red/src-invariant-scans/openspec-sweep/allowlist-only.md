# Fixture B — mirrors `openspec/specs/` with ONLY allowlisted version-history mentions

V1 → V2 cited `package-root-containment` here; retired at ADR-0077 archive-sync.
(Previously: this scenario cited `REQ-PRC-04` directly; re-pointed at archive-sync.)
V2 → V3 (Previously: rejected `source-outside-package`; narrowed away, no successor.)

Every retired-term mention above sits behind a version-history marker on its OWN line, so
the sweep LOGIC must credit the allowlist and NOT flag this fixture. [red-fixture]
REQ-FTG-06.4 pairing; read as TEXT by fit-43's red-proof only.
