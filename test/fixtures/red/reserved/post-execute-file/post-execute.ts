// Deliberately-red fixture (FIT-16 red-proof, REQ-RLN-01.3) — the pair-triangulation
// sibling for pre-execute-file, proving neither reserved name's coverage is a
// mutant-surviving accident of only ever testing the other half of the pair.
export {};
