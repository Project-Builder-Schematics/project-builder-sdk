// REQ-CST-04.2.3 / REQ-PRM-01: `node:vm` is a denied capability primitive (B4's fold — a
// module-specifier surface node, governed by origin admission, not a specifier special case).
import { Script } from "node:vm";

