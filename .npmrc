# npm's CLI refuses to publish without a token (ENEEDAUTH) even though the local
# Verdaccio accepts anonymous publishes — this token is only ever sent to localhost.
//localhost:4873/:_authToken=local-fake-token
